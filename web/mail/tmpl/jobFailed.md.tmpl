## Operation fehlgeschlagen

Die geplante Operation **{{ .Op }}** für **{{ .Course }}/{{ .Assignment }}** ist
**fehlgeschlagen**.

- **Geplant für:** {{ datetime .RunAt }}
- **Fehler:** {{ .Err }}
{{ if .Log }}
Ausgabe bis zum Fehler:

```
{{ .Log }}
```
{{ end }}
Bitte prüfen Sie die Konfiguration und Ihren GitLab-Token in glabs und planen Sie
die Operation bei Bedarf neu.
